# 静かなる深海のクジラ ── 計画書

Binance の BTC/USDT を、深海としてリアルタイムに描く。約定と板だけを入力に、
一時間眺めても飽きない画をつくる。

作り方の骨格は `amanesf/jellyfish`（『海月ノ箱』）を踏襲する。固定ステップの
シミュレーション、絵づくりを担うポストチェーン、そして**実装より先に測定基盤を
置く**という順序。参照は同リポジトリの `plan.md` と `app/src/`。

**スマートフォンの縦画面が主戦場**であり、これは制約ではなく前提である（§2）。

---

## 1. コンセプト

### 縦軸は価格である

この一点にすべてが乗っている。深さでも時間でもなく、価格。

- 板の壁は**その指値の高さ**に座る
- 約定は**約定した価格の高さ**に現れる
- 価格が上がれば**海全体が沈んでいく**（カメラが6秒遅れて追う）

「買いが強い」がメーターの数字ではなく、**海がゆっくり沈みはじめる**という体験に
なる。これが最も読みやすく、同時に最も美しい。

そして縦長の画面は、この軸にとって理想的である。価格がスマートフォンの長辺を
得て、クジラの体は狭い短辺を突き抜けて画面外へ消える。

### 物理スケールは解かせない。金融スケールは厳密に

画面に**既知の大きさを持つ物体を一つも置かない**。直線も、人工物も、比較対象も。
クジラが10mなのか300mなのかは永久に解けず、目は解こうとし続ける。

一方で価格は**ドル単位で正確に読める**。右端に価格軸、壁には引き出し線つきの注釈。
曖昧さと厳密さは対立しない——**どの軸を曖昧にするかを選ぶ**だけである。

### 唯一の成功条件

**一回の鑑賞で語彙を使い切らせないこと。** 大抵の生成アートは90秒で全パターンを
見せ切る。§2 はすべてこの一点への攻め手である。

---

## 2. 飽きなさの十の核

| # | 核 | 機構 | 状況 |
|---|---|---|---|
| 1 | スケールの手がかりを奪う | 既知サイズの物体を置かない。引き算のみ | 実装済 |
| 2 | 不完全な開示 | 全身を見せない。彷徨う灯りが皮膚の一部を数秒だけ照らす | 実装済 |
| 3 | セッションより長い語彙 | 毎秒／数秒／分／時間の階層。閾値6倍の「大クジラ」は出ないセッションもある | 部分実装 |
| 4 | 生理リズムをデータの下に敷く | 呼吸40秒・潮汐・カメラ60秒。**凪いだ市場でも作品は死なない** | 実装済 |
| 5 | 創発 | Boids。群れが自発的に割れ、再結合し、パニックが伝播する | **未** |
| 6 | 動く単一光源 | 有限の形から無限の画を得る唯一の方法 | 実装済 |
| 7 | 生きたカメラ | 慣性を持ち、0.3秒**遅れて**怯む。「見せられている」→「そこにいる」 | 実装済 |
| 8 | 色を振らず境界を動かす | 上層に朱、深層に青緑。圧力は**境界の高さ**だけを動かす | 実装済 |
| 9 | 蓄積 | 露出が activation に40秒で追従する。静かな海は沈み、活発な海は持ち上がる | 実装済（§13.5-6） |
| 10 | 三段のディテールと意図的な不完全さ | 場／一筆／粒。清潔さはCGに見え、粒状は撮影に見える | 実装済 |

**⑤と③が作品の格を決める**が、この二つだけ工数が跳ねる。

### 音（未着手）

無音のアンビエントはスクリーンセーバーで、音が付くと**部屋**になる。板の不均衡を
30〜60Hzのうねりに、約定レートを粒状のヒスに、大口を正弦波のクリックと8秒の
テールに。ブラウザの制約でどのみちクリック起動なのでオプトインにできる。

---

## 3. 生態系とデータの対応

| 役 | データ | 描画 | 場への作用 |
|---|---|---|---|
| オキアミ | < 0.1 BTC | 速度方向に伸びる異方性ストリーク。**せん断で光る** | 場そのもの。地の色を担う |
| マリンスノー | （個体数の16%） | 自ら光らない。灯りに照らされたときだけ見える | 光源の位置を報告する唯一の population |
| 魚 | 0.1 BTC 〜 閾値 | 5〜11匹の群れ。紡錘形・二叉尾・個体別の尾振り | 航跡を残す。**魚より航跡が本体** |
| クジラ | 閾値以上 | ナガスクジラ1匹。深部。全身は見えない | ランジフィーディング＝**吸い込み** |
| 板の壁 | depth20 | クジラの質量。**近く・大きく・暗く** | 重力レンズ（ポスト全画面） |

閾値は固定値ではない（§9）。

---

## 4. 水は本物の流体である（`scene/fluid.ts`）

カールノイズは30秒で壁紙になる。理由は明確で、**統計的に定常だから**——そこで何が
起きても場は変わらない。歴史がないので事件もない。

だから 160² の速度場を実際に解く。セミラグランジュ移流、渦度保存、6秒で減衰。

- **航跡が残る**。魚は2秒で横切り、抉った回廊は8秒後もまだ巻いている
- **渦が干渉する**。二つの衝撃は足し算にならず**編み合わさる**。作者は書いていない
- **同じ状態に二度とならない**。場の状態＝起動以来の全市場履歴

圧力投影は入れていない。**わざと非圧縮でない**——プランクトンは寄り集まるもので、
2パス払って買える写実をこの絵は求めていない。

渦度保存項は「粒子が漂う」と「水」の差そのものである。これが無いと航跡はただ薄れる
直線になり、あると縁が巻き上がって剥離する。

---

## 5. 光はせん断から来る（`scene/field.ts`）

渦鞭毛藻は**水が歪められたときに発光する**。夜の船の航跡が光り、静水が光らないのは
そのためである。この作品もそれに従う。

- 静水のオキアミは 0.042、航跡の中は 1.45。**比は約80:1**
- ゆえに**市場が何かをしている場所だけが光る**
- ブルームの閾値 0.48 は静水の上・航跡の下。静かな9割は暗く鋭いまま

**この比を潰すと画面全体が灰色に浮き、深海が深海でなくなる。** ブルーム閾値を
下げるのがこの絵を壊す最も簡単な方法である。

粒子は点ではなく**自分の速度方向に伸びる四角形**。静止時は丸い塵、ジェット中は線。
コストは頂点1個で、質を最も大きく動かすレバーである。

---

## 6. クジラ（`scene/whale.ts`）

### 描く。ただし全身は決して見せない

「不在として描く」案は却下した。**不在は一発ネタ**で、意味が分かった瞬間に消費が
終わる。ほぼ解けてしかし決して解けない身体のほうが無限に強い。

- フレーム幅より長く、両端は常に画面外
- 自ら発光しない。見えるのは**縁の散乱**と、灯りが当たっている数メートルだけ
- 鑑賞者は20分かけて、**一度も見ていないクジラ**を組み立てる

### 質量は明るさではない

板が厚くなるとクジラは**近く・大きく・暗く**なる。より多くの場を遮り、より多くの
発光を消す。**買い板が育つのを見ることは、光が消えていくのを見ること**である。
この反転が作品の不穏さの出どころ。

深度は**その壁が中値からどれだけ離れているか**。タッチに張り付いた壁は迫り、遠い
壁は沈む。近いものが「もうすぐ効く」ものである。

### ナガスクジラであること

イルカに見えた原因は3つあり、すべて修正済み。

| 誤り | 正 |
|---|---|
| 吻が尖って絞られていた | **平面視で尖り、側面視で平たい**。頭部断面を縦に潰し横に広げる |
| 最大胴回りが中央後方（オタマジャクシ） | **鼻先から38%** |
| 背びれが無い | **体後方3/4の小さな鎌形**。最強の識別キュー |

加えて、右下顎だけが白い**非対称色素**（灯りが頭に当たったときのみ見える）、
尾柄の急激な細まり、腹側の畝。

### 泳ぐ。そして食べる

- 定位しない。画面幅より少し広い軌道を往復する（**巻き戻らない**——反対の縁から
  現れるのは別個体であり、個体は1匹しかいない）
- 優勢な側の壁を体現し、優劣が入れ替わると**10秒かけて新しい価格帯へ泳ぐ**。
  データの不連続が、画面で最も読みやすい運動に変わる
- クジラ級の約定で**ランジフィーディング**：喉の畝が頭より大きな袋に膨らみ、
  前方の水が**内側へ吸い込まれる**

吸い込みは両方の読みで正しい。ナガスクジラのランジは吸気であり、大口の攻撃的注文は
板を押しのけるのではなく**食う**。オキアミが渦を巻いて口へ吸い込まれる画は、
外向きの爆発では買えない。

---

## 7. 一つの出来事を三つの速度で

同時に放って速度を変えると、演出ではなく**物理**に見える。

1. ~~クジラの脇腹の閃光 — 150ms~~ → 1.5秒かけて膨らむ緩い明暗へ（§13.7）
2. ~~ポストの屈折リング — 0.6秒~~ → **撤去**（§13.7）
3. 流体に注入された渦 — **7秒**かけて減衰し、粒子が螺旋を描いて飲まれる

すべて**アタック即座・ディケイ緩慢**。対称なエンベロープは「再生されたアニメーション」
に見える。

**ただし「速い」には下限がある。** ①②は観察で却下された（§13.7）。他のすべての運動が
秒単位で測られる画面に、0.2秒で現れて消える明るいリングを置くと、それは物理ではなく
**エフェクトのレイヤー**として読まれる。三つの速度という考えは*遅い*ものについて
正しく、速いものについては誤りだった。

---

## 8. 計器はダイエジェティックである（`ui/hud.ts`）

作品に管理画面を貼ると両方が死ぬ。解決は**計器を作品世界の内側に置く**こと——
画面は深海調査艇の窓で、これはその観測オーバーレイである。ヘアライン、等幅の数字、
目盛り、対象を指す引き出し線。ネイチャードキュメンタリーが個体に注釈する作法。

三段階の可読性：

| 関与 | 読めること | 手段 |
|---|---|---|
| 一瞥 0.5秒 | 買売の優劣、活況 | 色境界の高さ、群れの向き、密度 |
| 数秒 | 価格、圧力、レート | 価格・圧力バー・約定/秒 |
| 読む | 誰が何をしたか | `BID WALL 128.4 BTC @ 96,240`、テープ |

**凡例は必須。** 作者だけが知っている対応表は可読性ではない。20秒表示して畳まれる。

### 画面は水である

凡例の開閉は**自前の三角ボタン**が持つ。画面全体のタップはもっと良いことに使う——
**水に触れると散る**。押しているのは粒子ではなく**流体場**なので、反応に重さがある。
水が膨らんで避け、縁が巻き、指を離した数秒後まで収まらない。粒子を直接動かしていたら
即座で軽い、はるかに劣る反応になっていた。

---

## 9. 閾値は定数ではなく制御量である（`market/ocean.ts`）

実測：Binance BTC/USDT の aggTrade は 1日60万〜150万件、うち **5 BTC以上は
0.02〜0.05%＝1日150〜600件**。平均2.5〜10分に1回、しかも極端にクラスタする。

教科書的には 5 BTC が正しく、実用では役に立たない。**午前4時に開いた人は、この
作品で最大の出来事を一度も見ない。**

だから閾値 W は**目標発生率1分/回に対する制御量**である。時定数150秒の遅い積分制御、
0.8〜40 BTC でクランプ。通常時 **1.5〜2 BTC** に落ち着く（実測で 1.91 BTC）。

可読性は落ちない。表示は常に実際の BTC 数量であり、加えて凡例が現在の W を示すので
**「今この市場で大口とは何BTCか」**が読めるようになる。固定値には言えないことである。

同じ理屈が板の基準値にも効いている。日曜の最大の壁は40コイン、水曜は400コイン。
固定の分母は日曜に黒く、水曜に飽和する。

---

## 10. 測って直す

`scripts/capture.js` が実機サイズで実ブラウザを駆動し、数秒おきに静止画を撮る。
`scripts/interact.js` はタップ前・直後・沈静後の同じ切り抜きを撮る。

**書いている最中には一つも見えず、撮って初めて分かった不具合：**

| 症状 | 原因 |
|---|---|
| 全画面が虹色に縁取られる | 色収差が6倍強すぎた。**虹として名指せる時点で数倍過剰** |
| 地が黒から浮いて灰色 | 水面の微光が強すぎ、ブルーム閾値が低すぎた |
| 海の真ん中に薄い矩形 | 消したはずの凡例が `opacity:0` のまま `backdrop-filter` で背景をぼかし続けていた |
| 胸鰭が巨大な平板／体側がファセット化 | **SDFの単位バグ**。xだけ体長で正規化し y,z は世界単位のまま。鰭が `R*0.62*L` になり、同時に距離場が非Lipschitzになって球трейスが踏み越えていた |
| 魚が流星の逆に動く | フラグメントの符号。`pow(1.0-head,3)` で**尾の先端が最も明るかった** |
| 画面右に縦の継ぎ目 | 背景板を原点のカメラに合わせて一度だけサイズ決めしていた。カメラは常に漂う |
| クジラが平たい板に見える | 舷側から見た円柱は帯である。ヨーで奥へ潜らせて解決 |
| タップで場に穴が開く | 強度3倍・持続2倍だった |
| クジラが見当たらない | 灯りが独立に彷徨い、体の近くにほとんど来なかった |
| 近づくと**ギザギザの縁を持つ平らな塊**になる | 球トレースのヒット閾値と最小ステップが**絶対値**（0.02／0.05）だった。体が画面を占めるほど、体側をかすめる光線が微小ステップを繰り返して反復を使い切り、「外れ」として返る。距離場は近いので縁のフェザーが被覆1と解釈し、**シルエットの内側をフラット色で塗った**。閾値と歩幅を胴回りの比率にすると、距離によらず必要反復数が一定になる |
| クジラがX線写真に見える／白い塊に見える | 明るさの配分が逆だった。照らされた肌 0.02 に対し縁の散乱 0.18 ——**体を描いていたのは縁**で、内側には何もなかった |

**教訓**：符号・単位・不可視要素の副作用は、ソースを読んでも見つからない。

---

## 11. 残件

| 優先 | 項目 | 効果 | 工数 |
|---|---|---|---|
| 高 | **⑤創発（Boids）** 群れが自発的に割れ・再結合し、パニックが伝播する | 極大 | 大 |
| 高 | **③希少イベントの階層** 10分／1時間／出ないかもしれない出来事 | 極大 | 中 |
| 中 | ~~**⑨蓄積**~~ 露出で代替した（§13.5-6）。物としての堆積は保留 | 大 | 中 |
| 中 | **音** サブベースの歌、粒状のヒス、8秒のテール | 大 | 中 |
| 中 | **本物のBinanceデータでの検証** | — | 小 |
| 低 | 遠い水面のコースティクス | 中 | 中 |
| 低 | 実機での発熱・フレームレートの実測 | — | 小 |

### 検証の穴（正直に）

**開発環境からは Binance に接続できない。** すべての判断は合成潮流（同じPareto裾を
引く代替テープ）に対して行っており、本物の板とテープでの見え方は**一度も確認して
いない**。特に以下は実データで崩れる可能性がある。

- 板の壁の基準値の収束（合成では壁が規則的すぎる）
- 閾値制御の挙動（実際のクラスタ性の下での応答）
- 約定レートが実測で桁違いに高い時間帯の密度

また、キャプチャは負荷の都合で**粒子数を実機の1/10前後**に落としている。密度と
明るさの最終判断は実機で行う必要がある。

---

## 12. 運用

```
app/          Vite + TypeScript + three.js。これが作品
scripts/      capture.js（静止画）／interact.js（操作の検証）
```

```sh
cd app && npm ci && npm run dev
cd app && npm run build
node scripts/capture.js  --shots 4 --every 8
node scripts/interact.js
```

`main` への push で GitHub Pages に自動デプロイされる
（https://amanesf.github.io/kujirabtc/）。

キャプチャ用のクエリ：`?res=96&scale=0.5`。ヘッドレスは SwiftShader で CPU 描画に
なるため、実機の粒子数と解像度では1フレームに分単位かかる。構図・配色・レイアウトは
1/10の粒子数でも正しく、静止画で見るのはその三つである。

---

## 13. 次の作業 ── 診断と計画

観察から出た三つの指摘（大口ボタンで画面端の色が壊れる／食べているように見えない
／端で見切れる）を追った結果、**前の二つは同じ一つの原因**だった。ここに残す。

### 13.1 根本原因：クジラの座標系と水の座標系が繋がっていない

カメラは z=30、fov 38°。フレームの半幅は距離で変わる。

| | 深度 | 半幅 |
|---|---|---|
| 水（流体・オキアミ・魚） | z ≈ 0 | **4.75** |
| クジラ | z ≈ −70 | **15.8** |

`lungeAt()` が返す口の位置は**クジラの空間**にある（`cruise` ±21 に体長 ~±20 が
乗り、最大 ±45）。それを `main.ts` はそのまま下流に渡している。

- `fluid.add({x: at.x})` — 流体の定義域は **±4.75**。±45 は10領域分の外側で、
  ガウシアン減衰 `exp(-r²/radius²)` は r=40 で実質ゼロ。**大口を押しても水は
  一度も反応していない。** これが「食べているように見えない」の主因
- `ndc.set(at.x, at.y, -6).project()` — **z=−6** で投影している（クジラの深度では
  ない）。ndc.x ≈ 7.7、つまり画面外へ大きく外れた位置に衝撃波リングの中心が座る。
  リング幅は `0.030 + 半径×0.09` なので半径1.5では**幅0.17の帯**になり、それが
  画面端を舐める。さらに `abyss.ts` は3チャンネルを別々の変位で別々に `clamp`
  するため、端で色が分離する
- `observer.push(at.x, ...)` も同じ桁の誤り

実測（390×844、クリック前後、左端12px×500行の平均RGB）：

```
クリック前   left=(2.62, 2.64, 2.66)    ニュートラル
+500ms      left=(15.52, 11.67, 10.61)  6倍に増光し、赤へ偏る
+650ms      left=(1.27, 1.06, 0.97)     元より暗い
中央は 9.9 → 9.6 でほぼ不変
```

### 13.2 派生して見つかった欠陥

- **重力レンズが体に乗っていない。** `ndc.set(ws.cruise * 0.4, …)` ——
  実深度で投影しているなら遠近は既に効いており、×0.4 は二重掛け。質量が空間を
  曲げる場所と、その質量がある場所が一致していない
- **灯りが二箇所にある。** `lx = ws.cruise * 0.55` を水（`field.setLight`）と
  クジラ（`whales.setLight`）の両方に同じ数値で渡している。空間が違う以上、同じ
  数値が両方で正しいことはありえない。§5 が主張する「一つの光源が二つの仕事を
  する」は、実装では破れている
- **口が無い。** `sdWhale` の構成は body / flukes / dorsal / pec / pleats で、
  顎の開口がどこにもない。今の「ランジ」は喉の袋が下へ膨らむだけである。捕食に
  見えない最大の理由はおそらくこれ
- **呑まれるものが何も消えない。** オキアミは wrap して不死（§5 の設計通り）、
  魚は散るだけ。食べた証拠がフレームに残らない
- **`uExposure` が死んでいる。** `abyss.ts` と `field.ts` の双方に宣言があり、
  双方に `col *= uExposure` まで書かれていて、どこからも設定されていない。値1の
  まま配線だけが通っている全画面レバー

### 13.3 作業（この順で）

1. **座標系の統一。** クジラ空間 ↔ 水空間の変換を一箇所で定義し、定数 0.4 / 0.55
   を廃して `tan(fov/2)·(camZ − depth)·aspect` から導く。口の位置を水空間へ変換
   してから流体・衝撃波・カメラへ渡す。衝撃波はクジラの実深度で投影する。あわせて
   `abyss.ts` の変位を画面端で smoothstep により0へ落とす（3チャンネルが同じ場所を
   読むので色分離が原理的に起きなくなる。「衝撃波が枠の端に届くと枠の存在を教えて
   しまう」という同ファイルの主張とも一致する）。ここでレンズを体に乗せ、灯りを
   一点に戻す
2. **大口シーケンス。** 現状は `lunge=1` から2.4秒の線形減衰という一発で、加速
   しながら口を開けており順序が逆である。五相に分ける

   | 相 | 秒 | 内容 |
   |---|---|---|
   | 狙う | 0–1 | 中央が背中側なら旋回。捕食時の旋回は速くてよい ── 巡航で26秒を要したのは先端速度と前進速度の比の問題であり、8倍速ならその比は保たれる |
   | 助走 | 2–4 | 加速。ストロークは effort 連動なので自動的に深く速くなる |
   | 開口 | — | **中央到達で発火**（タイマーではなく位置で）。端で見切れる問題はここで構造的に消える |
   | 呑込 | 1.5 | 袋が開くと**急減速**。膨らんだ喉はパラシュートであり、実際のナガスクジラは突進後ほぼ停止する。ここが最も「食べた」に見える瞬間 |
   | 回復 | 8 | 袋が抜けて巡航へ戻る |

   あわせてボタンを `突進` から **`大口`** に改める
3. **口をつくる。** `sdWhale` に蝶番付きの下顎。head 領域（t > 0.78）の下半分を
   サンプリング座標ごと gape 角（最大 ~65°）で回す。口腔内は元来無照明なので黒く
   落ちる＝穴に見える
4. **呑まれる。** 魚は口の中で消える（一瞬光ってから）。オキアミは口の体積内で
   減光する（`setLight` と同じ形の uniform を `field` へ）
5. **タイトルと、価格の降格。** 画面で最も大きく最も彩度が高いのは価格であり、
   その色相は海の ember 側＝売り圧と同じである。作品の色彩言語で「売り」を意味する
   色を、UIが最大サイズで使っている。上部に **「静かなる深海のクジラ」** を小さく
   字間広く置き、価格は一段小さくほぼニュートラルにして、方向は色ではなく細い
   ティックで示す。飽和した寒色／暖色は海だけの語彙として予約する。あわせて数px
   インセットの内枠ヘアラインを一本 ── §8 は「潜水艇の測量オーバーレイ」という
   文法を宣言しているのに計器の枠がなく、これだけで画面が壁紙ではなく舷窓になる
6. **露出を市場へ繋ぐ。** 死んでいる `uExposure` を `agitation` に、30〜60秒の
   時定数で繋ぐ。静かな市場では全体が沈み、活発になると持ち上がる。数分単位でしか
   気づかない遅さが肝。**⑨蓄積が求めていた「40分後が1分後と違う」を、新しい状態を
   一つも足さずに得られる**
7. **肌。** 現状 `barnacle * 0.20` が基礎値 0.048 の4倍で支配的になり、灰色の岩に
   見えている。背を暗く腹を淡く（カウンターシェーディング。海洋動物の最も強い識別
   手がかり）、フジツボを 0.06 へ、狭いスペキュラを一項（岩と肉の差はほぼこれ）
8. **魚がクジラを避ける**（現状まったく無関係に泳いでいる。`scatter()` は既にある）。
   **HUD の衝突**（BID WALL のラベルがテープと重なる。表示条件 `top < 0.94` を
   フッター高 ~0.82 へ）

### 13.4 判断の記録

- **⑤創発（Boids）は次にやることではない。** Boids は持続する個体群を要求するが、
  魚は寿命 3〜4 秒の使い捨てであり（`life = 2.6 + power * 1.8`）、オキアミは
  10万体が常駐する代わりにエージェンシーを持たない（移流されるだけ）。⑤ は
  「既存の群れに三つの力を足す」ではなく「第三の個体群を新設する」である。さらに
  ⑤ が挙げる現象（分裂・再結合・パニックの伝播）は数十秒スケールで、3秒の群れでは
  既存の `scatter()` と見分けがつかない。加えて `shoal.ts` 自身が「魚は小さく、
  群れは横切るテクスチャであって登場人物ではない」と決めており、⑤ の見返りは
  魚が登場人物であることを要求する。天井の高さと着手順は別である
- **⑨蓄積は単独では要らない。** 変化は市場そのものが供給しており、静かな市場での
  保険は ④生理リズム（呼吸40秒・潮汐・カメラ60秒）が既に担っている。⑨ の効果は
  上記6で足りる
- **オキアミの深度成層は見送り。** 効果は大きいが博打であり、今の段階で払う賭金
  ではない
- 被写界深度（焦点面からの |z| でスプライトを広げて薄くする）と、HUD 全体の彩度
  の見直しは、入れるか未定

---

## 13.5 実施記録 ── §13.3 の八項目

すべて実装し、実測で確認した。以下は「何をしたか」ではなく「何が変わったか」。

### 1. 座標系（`core/space.ts` 新設）

クジラ空間の点をカメラで投影し、同じ画面位置に見える z=0 の点を返す一関数に
集約した。定数 0.4（レンズ）と 0.55（灯り）は廃止。口・体・灯りはすべてこれを
通る。あわせて `abyss.ts` の変位を画面端 0.10（アスペクト補正済み座標）で
smoothstep により 0 へ落とした。

実測（390×844、`scripts/interact.js`、左端12px×中央半分の平均RGB）：

```
        left                      mid
前      (10.91, 11.64, 14.34)     (10.27, 11.76, 14.55)
+1s     ( 9.84, 10.51, 13.31)     ( 9.27, 11.25, 14.44)
+3s     ( 8.91, 10.40, 13.58)     ( 7.60,  9.37, 12.45)
+5s     ( 8.35,  7.07,  8.74)     (25.12, 37.34, 44.27)   ← 呑込
+6s     ( 8.21,  6.75,  8.40)     (20.04, 30.12, 35.91)
+8s     ( 9.21,  8.42, 10.46)     ( 8.53,  9.96, 12.51)
```

端は最大でも1〜2割の範囲で動き、赤へ偏らない（旧：6倍に増光し赤へ）。**中央は
4倍以上に増光する**。すなわち出来事は画面の端ではなく、水の中で起きるように
なった。これが §13.1 の三症状のうち二つの直接の答である。

### 2〜4. 五相のランジ、口、呑まれるもの

`WhaleState.phase` が `aim → run → engulf → recover` を進む。開口は**タイマー
ではなく `|cruise| < 3.5` で発火**するので、端で見切れることは構造的に起きない。
`engulf` で `boost` を 0.32 秒の時定数で −0.82 まで落とす——膨らんだ喉は
パラシュートである。狙いと助走の間だけ旋回速度を8倍にした（先端速度と前進速度の
比は保たれるので、§6 で 26 秒に落とした理由は壊れない）。

口は SDF から**引き算**で作った。t=0.78 の蝶番より前・口蓋より下・下顎平面より上、
という三つの半空間の共通部分を body から減じる。平面距離の max なので Lipschitz
であり、サンプリング座標を回す方法（過去に鰭の単位バグと同じ壊れ方をする）は
採らなかった。口腔内を照らすものは何もないので、そのまま穴として落ちる。

呑まれるものは二種。魚は `shoal.consume()` で口へ引き込まれ、0.3秒だけ強く光って
消える。オキアミは口の体積内で減光する（`field.setMouth`）——この海の個体群は
不死である（§4の設計）から、消滅は暗転としてしか書けない。

### 5. タイトルと価格の降格

上部に「静かなる深海のクジラ」を 10px・字間 0.48em で。価格は 30px の飽和色から
22px のニュートラルへ落とし、方向は色ではなく2pxのティックが持つ。飽和した寒色／
暖色は海の語彙として予約された。内枠ヘアラインを一本入れた——§8 が宣言する
「潜水艇の舷窓」は、枠を一本引くまで壁紙のままだった。

### 6. 露出

死んでいた `uExposure` を agitation に時定数40秒で接続。field 側はブルーム閾値の
**手前**に効くので活況が発光量そのものを動かし、post 側は同じ振れの45%を
トーンマップの**後**で黒レベルとして受ける（二重に掛けて震えないため）。
⑨が求めていた「40分後が1分後と違う」を、新しい状態を一つも増やさずに得た。

### 7〜8. 肌、魚、HUDの衝突

フジツボ 0.20 → 0.06（基礎値の4倍で支配的な灰色の岩になっていた）、
カウンターシェーディング（背 0.60／腹 1.30）、狭いスペキュラを一項。
魚は毎フレーム体から穏やかに散り、口が開いていれば呑まれる。
BID WALL ラベルの表示条件は `top < 0.94` → `0.80`（フッター上端）。

### 残っていること

⑤創発と③希少イベントの階層は手つかずのままである（§13.4 の判断は変えていない）。
音も未着手。**本物の Binance データでの検証は依然として一度もできていない**——
この環境から取引所に到達できず、上の実測もすべて合成潮流に対するものである。

---

## 13.6 クジラと水が噛み合っていなかった件

「クジラのエフェクトとアニメーションとオキアミが噛み合っていない／造形と
アニメーションの美しさが足りない」という指摘を追った記録。

### 水は一度もクジラを知らなかった

この作品の流体への力は**すべて事象**である——約定が来る、場が叩かれる、減衰する。
**画面より長い体が横切っているという事実に駆動される項が一つも無かった。**
だからクジラは泳ぎ、オキアミはそれに気づかない。これが「噛み合っていない」の実体。

`fluid.setSwimmer()` を新設し、毎フレーム更新される**定常項**を二つ置いた。

- **頭**：進行方向へ押す。効率よく言えば船首波で、目に見えるのは*前方でオキアミが割れる*こと
- **尾**：**ストローク位相に同期して左右交互に横へ押す**。渦度保存項を持つ場はこれに
  対して**一拍ごとに渦を放出**する。左、右、左——体の後ろに渦列が伸び、水が覚えている
  6秒のあいだ残る。**この航跡はどこにも書かれていない**。尾が水にする仕事の帰結であり、
  クジラとプランクトンが同じ海にいることの、画面上で最も強い証拠である

定常項の到達値は押しの約6倍（減衰時定数）なので、値は意図的に小さい。

### 造形：三つの誤り

1. **反復切れ**（§10の表に追加）。近距離のギザギザは陰影の問題ではなく、
   全ピクセルが収束に失敗していた
2. **明るさの配分が逆**。照らされた肌 0.02／縁 0.18。26倍に上げ（ソフトクランプで
   ブルーム閾値 0.48 の下に留める）、縁を半分に落として、ようやく「暗い体に一つの
   明るい斑」になった
3. **灯りの減衰が緩すぎた**。0.075/単位では体長40単位で4倍しか差がつかず、全身が
   一様に光る＝**面に勾配が無い＝形が無い**。灯りを体に固定した修正（§13.5-1）が、
   同じ一撃で §2 の「不完全な開示」を殺していた。0.13/単位で13倍になる

あわせて：肌に近距離用の細かい粒（`fbm(p*3.1)`）、腹側の畝を胴回りの1%から2.4%へ
（ナガスクジラの近距離で最も美しい造作であり、1%は何処からも見えない）、目を一つ、
尾びれに**迎え角**（既に適用している波の傾きを尾の位置で取って剛体回転させる。
翼が水を捉えるのは角度によるのであって、平板を引きずっても推進にはならない）。

### 巡航深度を戻した

質量1で −52、つまり常時画面を埋めていた。突進したときに行き場が無いのは、
尾振りに余力を持たせた理由と同じ問題である。巡航 −68、接近で +40（−38でクランプ）。
**「そこにいる」と「来る」の差が見えるようになった。**

---

## 13.7 観察による五つの却下と修正

「衝撃波と一瞬明るくなる演出が気持ち悪い／クジラは反射を減らして縁が光るほうが神秘的／
衝撃波のときに魚が突然現れてバグみたいに動く／左向きの大口が手前に来ない／
オキアミが食べられた感じがしない」——五つとも実体があった。

### 速い演出は物理ではなくレイヤーに見える

衝撃波リング（`effects/abyss.ts` の変位＋発光）と脇腹の閃光（150ms）を撤去した。
根拠は §7 の「一つの出来事を三つの速度で」だったが、その主張は**遅い側について
正しく速い側について誤り**である。残ったのは常に良かったほう——7秒かけて十万の体を
口へ巻き込む渦。7秒で終わる出来事は7秒間感じられ、0.2秒で終わるものはグリッチに見える。
観測者の怯み（0.3秒遅れ）だけは残した。これは画面が叩かれるのではなく**体が気づく**動きである。

### 縁が描き、肌は材質だけを言う

明るさの配分は両方向に外していた。縁ばかりだとX線写真（輪郭だけで中身が無い）、
肌ばかりだと**濡れた岩に懐中電灯**で、正確だが少しも神秘的でない。
肌の利得 26→13（さらに強いソフトクランプ）、スペキュラ 4.0→0.7、縁 0.045→0.085。

### 魚が湧いていた

クジラ級の約定は**その約定価格に魚群も生成していた**。何もない水から5〜11匹が
衝撃波と同時に現れ、数秒後に口へ引ったくられる。外から見ればまさに見た通りのもの——
合図で生成され消される物体。**クジラになるほど大きい約定は、クジラそのものである。**
生成を止め、捕食の引き込みも 9→3.5 単位/秒、閃光 3.0→0.8 に落とした。

### 左右で別の動物だった

旋回はヨーに π を足していた。これは進行方向を反転させるが、**同時に奥行き方向の
姿勢も反転させる**。つまり片道は頭が手前を向き、もう片道は暗い奥を向いていた。
交互に現れる二頭の別の動物であり、作品が語っているのはそのうち一頭だけだった。

回転ではなく**鏡映**にすると直る。理由は恒等式ひとつ：`cos(π−a) = −cos a` だが
`−sin(π−a) = −sin a`。π−YAW は進行方向が逆で**奥行きの姿勢は同じ**である。

### 口が画面の外にあった

開口の発火条件が「体の中心が中央に来たら」だった。中心は軌道に乗る点で、頭はヨーの
ぶんだけ体長ほど前——20世界単位、ランジ時の深度ではフレーム半幅の約2倍である。
**つまり顎が開くのは確実に画面の右外だった**。吸い込みも、顎がオキアミに落とす影も、
呑まれる魚も、全部そこで起きていた。判定を口の位置に変えた。

### 吸い込みが続かなかった

吸引は開口フレームに一発撃つインパルスで、残り寿命の二乗で減衰する——**顎が最も
開いている頃には引きはもう衰えている**。だからオキアミは漂うだけで、食べられた感じが
しなかった。口が開いているあいだ**定常の内向き放射**を置いた（`setSwimmer` に radial）。
1.5秒のあいだ水は顎へ向かって加速し続け、場は漏斗になる。あわせてオキアミの消滅を
ガウシアンから**境界のある smoothstep** に変えた。顎には縁がある。ガウシアンには無い。
